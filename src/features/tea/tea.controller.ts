import { Controller } from '@nestjs/common';

@Controller('tea')
export class TeaController {
    // This controller will handle routes related to tea
    // You can define your endpoints here, e.g., GET, POST, etc.
    // For example:
    // @Get()
    // findAll() {
    //   return this.teaService.findAll();
    // }
}
